---
phase: 05.1-visual-experience-overhaul
plan: 02
subsystem: ui
tags: [react, css-modules, glass-morphism, typography, playfair-display, amber-gradient]

requires:
  - phase: 05.1-01
    provides: CSS custom properties tokens (--glass-bg, --glass-border, --glass-border-subtle, --blur-glass, --glow-amber, --glow-amber-strong, --serif updated to Playfair Display, ambient layer keyframes)

provides:
  - Glass-morphism surface treatment on all 11 component CSS modules and 4 page CSS modules
  - Amber gradient + glow fill on MeterBar and VoteTally fill bars
  - Choice button interaction spec (glass default, glow hover, bright locked ring, dimmed grayscale)
  - Typography upgrade: scenario/consequence/explanation text at 21px Playfair Display 400
  - FrameworkProfile .frameworkName at Playfair Display 28px 700 (heading tier)
  - CSS animation-delay stagger removed from FrameworkProfile (Framer Motion prep for Plan 03)
  - All font-weight: 600 migrated to 700 (two-weight system enforced)
  - z-index layering (position relative z-index 1) on all page wrappers for ambient layer

affects: [05.1-03, visual-qa, polish]

tech-stack:
  added: []
  patterns:
    - "Glass recipe: background var(--glass-bg) + backdrop-filter blur(16px) + border var(--glass-border) + border-radius 12px + box-shadow inset highlight"
    - "Gradient fill: linear-gradient(90deg, #b45309 0%, #f59e0b 100%) with box-shadow glow"
    - "Two-weight system: Inter/Playfair 400 body, 700 for all labels/headings — no 600"
    - "Prose tier: 21px Playfair Display 400 line-height 1.75 for scenario/consequence/explanation"
    - "Heading tier: 28px Playfair Display 700 line-height 1.2 for frameworkName, page titles"
    - "Input exclusion: inputs keep var(--bg-surface) + #2e303a border — NOT glass"
    - "Page z-index layering: position relative z-index 1 on all page containers for ambient layer paint-through"

key-files:
  created: []
  modified:
    - src/components/ScenarioCard.module.css
    - src/components/ConsequenceReveal.module.css
    - src/components/ContentNote.module.css
    - src/components/MeterBar.module.css
    - src/components/FrameworkLabel.module.css
    - src/components/VoteTally.module.css
    - src/components/WorldStatePanel.module.css
    - src/components/PlayerRoster.module.css
    - src/components/CityPlaceholder.module.css
    - src/components/FrameworkProfile.module.css
    - src/components/TimerDisplay.module.css
    - src/pages/Landing.module.css
    - src/pages/HostSetup.module.css
    - src/pages/Host.module.css
    - src/pages/Play.module.css

key-decisions:
  - "Inputs excluded from glass treatment per UI-SPEC: inputs keep var(--bg-surface) solid bg and literal #2e303a border for readability"
  - "FrameworkProfile CSS animation-delay stagger removed entirely (not converted to CSS instant) — Framer Motion stagger in Plan 03 replaces it"
  - "ConsequenceReveal: added new .card class for glass wrapper; existing .consequence and .container left unchanged for Plan 03 JSX to wire"
  - "ContentNote: added .card class for glass wrapper; .overlay stays as full-page dark bg"
  - "TimerDisplay: no glass treatment per UI-SPEC; only font-weight 600->700 on .countdown"

patterns-established:
  - "Glass recipe is the single standard for all card/panel containers — do not mix old bg-surface + #2e303a pattern"
  - "font-weight: 700 is the only bold weight — 600 is eliminated from the design system"
  - "Amber gradient is the standard fill for all progress/bar elements (MeterBar, VoteTally)"

requirements-completed: [D-06, D-07, D-08, D-09]

duration: 8min
completed: 2026-03-26
---

# Phase 05.1 Plan 02: CSS Glass-Morphism and Typography Overhaul Summary

**Glass-morphism surface treatment, amber gradient fills, and Playfair Display typography applied to all 15 CSS module files (11 components + 4 pages), with font-weight: 600 fully eliminated.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-26T03:00:00Z
- **Completed:** 2026-03-26T03:08:00Z
- **Tasks:** 2
- **Files modified:** 15

## Accomplishments

- Applied glass recipe (backdrop-filter blur 16px, amber glass border, dark translucent bg, inset highlight shadow) to all card/panel containers: ScenarioCard, ConsequenceReveal, ContentNote, FrameworkLabel, VoteTally, WorldStatePanel, PlayerRoster, CityPlaceholder, FrameworkProfile, Landing .section, HostSetup, Host .endPanel, Host .reflectionCard
- Upgraded scenario and consequence prose to 21px Playfair Display 400 line-height 1.75; FrameworkProfile .frameworkName to 28px 700 (heading tier)
- Replaced flat amber fill on MeterBar and VoteTally with `linear-gradient(90deg, #b45309 0%, #f59e0b 100%)` with `box-shadow: 0 0 8px rgba(245,158,11,0.4)` glow; danger state uses red gradient
- Implemented D-08 choice button spec: glass default, amber glow on hover, 2px accent ring + strong amber glow on lock, grayscale dimmed state
- Removed CSS animation-delay stagger from FrameworkProfile (4 sections: sectionCard, conflictSection, leastUsedSection, choiceLog) and Play .reflectionSection — Framer Motion handles in Plan 03
- Eliminated all `font-weight: 600` instances from all 15 CSS files (two-weight system: 400 body, 700 labels/headings)
- Added `position: relative; z-index: 1` to all page containers and key wrappers for ambient layer paint-through

## Task Commits

1. **Task 1: Glass-morphism + typography on all 11 component CSS modules** - `9c5df1b` (feat)
2. **Task 2: Glass-morphism + typography on all 4 page CSS modules** - `6f4e3be` (feat)

## Files Created/Modified

- `src/components/ScenarioCard.module.css` - Glass card, upgraded choice buttons (glow hover + lock ring), 21px body
- `src/components/ConsequenceReveal.module.css` - New .card glass class, 21px consequence text
- `src/components/ContentNote.module.css` - New .card glass class, 700 weight on continue button
- `src/components/MeterBar.module.css` - Amber gradient fill with glow, red gradient danger, 700 weights
- `src/components/FrameworkLabel.module.css` - Glass badge recipe, 700 weight on name
- `src/components/VoteTally.module.css` - Glass container, amber gradient fill, 700 weights
- `src/components/WorldStatePanel.module.css` - Glass panel, 700 weights on actionBtn/scenarioTitle
- `src/components/PlayerRoster.module.css` - Glass playerCard with subtle border
- `src/components/CityPlaceholder.module.css` - Glass panel
- `src/components/FrameworkProfile.module.css` - Glass sectionCard/conflictSection, Playfair 28px frameworkName, 21px explanation, CSS animation stagger removed, glass-border-subtle on section dividers
- `src/components/TimerDisplay.module.css` - font-weight 600->700 on countdown only
- `src/pages/Landing.module.css` - Glass .section, 700 weights, z-index 1 layering
- `src/pages/HostSetup.module.css` - Playfair heading, roomCode text-shadow glow, glass-border-subtle on inactive round btns, 700 weights, z-index 1
- `src/pages/Host.module.css` - Glass endPanel + reflectionCard, Playfair endText, 700 weights, z-index 1 on roundView/endView
- `src/pages/Play.module.css` - 21px reflectionQuestion/reflectionRoundQuestion, opacity 1 on reflectionSection (animation removed), glass-border-subtle on textarea, 700 weights, z-index 1

## Decisions Made

- Inputs (`.input`, `.reflectionTextarea`) explicitly excluded from glass treatment per UI-SPEC — keep `var(--bg-surface)` solid background and literal `#2e303a` border for readability
- `ConsequenceReveal.card` and `ContentNote.card` added as CSS classes ready for Plan 03 JSX to wire — existing `.consequence` and `.container` left structurally unchanged
- `TimerDisplay.module.css` receives no glass treatment (typographic element per UI-SPEC); only font-weight 600->700

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All 15 CSS module files carry glass-morphism, gradient fills, and corrected typography
- FrameworkProfile and Play .reflectionSection have CSS animation stagger removed — ready for Framer Motion stagger in Plan 03
- ConsequenceReveal .card and ContentNote .card classes defined — Plan 03 JSX needs to wrap content in these
- `npm run build` passes cleanly
- Plan 03 (Framer Motion animations) can proceed: page transitions, round fade-through, end screen staggered reveal

---
*Phase: 05.1-visual-experience-overhaul*
*Completed: 2026-03-26*
