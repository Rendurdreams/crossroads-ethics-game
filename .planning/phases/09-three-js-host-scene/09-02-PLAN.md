---
phase: 09-three-js-host-scene
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/components/KingdomScene.jsx
autonomous: false
requirements: [THREE-03, THREE-07]

must_haves:
  truths:
    - "After a round closes and world state updates, each landmark visually shifts to match the new meter value"
    - "Bridge glow intensifies/dims smoothly as trust changes; beacon light and rotation speed track courage; village window emissive tracks solidarity; fog density tracks awareness (inverted)"
    - "The 3D scene runs at stable 60fps on a standard laptop while Supabase subscription events fire"
    - "Extreme meter values (below 20 or above 85) trigger distinct dramatic visual events"
  artifacts:
    - path: "src/components/KingdomScene.jsx"
      provides: "Lerp-driven landmark reactivity + threshold events"
      contains: "MathUtils.lerp"
  key_links:
    - from: "src/components/KingdomScene.jsx Bridge"
      to: "worldState.trust"
      via: "useEffect sets targetRef, useFrame lerps material.emissiveIntensity"
      pattern: "targetEmissiveRef"
    - from: "src/components/KingdomScene.jsx BeaconTower"
      to: "worldState.courage"
      via: "useEffect sets targetRef, useFrame lerps spotlight intensity + rotation speed"
      pattern: "targetIntensityRef"
    - from: "src/components/KingdomScene.jsx VillageQuarter"
      to: "worldState.solidarity"
      via: "useEffect sets targetRef, useFrame lerps emissive opacity"
      pattern: "targetOpacityRef"
    - from: "src/components/KingdomScene.jsx FogController"
      to: "worldState.awareness"
      via: "useEffect updates FogExp2 density (inverted)"
      pattern: "FogExp2"
---

<objective>
Wire all 4 landmark components to world state with smooth lerp interpolation and dramatic threshold events. Validate 60fps performance.

Purpose: Makes the 3D scene reactive — the kingdom visually responds to collective player choices each round.
Output: Landmarks animate smoothly between states; extreme values trigger dramatic visual events; 60fps confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-three-js-host-scene/09-CONTEXT.md
@.planning/phases/09-three-js-host-scene/09-RESEARCH.md
@.planning/phases/09-three-js-host-scene/09-01-SUMMARY.md
@src/components/KingdomScene.jsx

<interfaces>
<!-- Performance pattern from research -->

From 09-RESEARCH.md Pattern 3 (critical for THREE-07):
```jsx
// Do NOT set React state inside useFrame — use refs + mutation
function BeaconTower({ courage = 50 }) {
  const lightRef = useRef()
  const targetIntensityRef = useRef(courage / 100 * 50)

  // Update target when prop changes (React re-render — rare)
  useEffect(() => {
    targetIntensityRef.current = courage / 100 * 50
  }, [courage])

  // Lerp toward target each frame — no setState, no React involvement
  useFrame((state, delta) => {
    if (lightRef.current) {
      lightRef.current.intensity = THREE.MathUtils.lerp(
        lightRef.current.intensity,
        targetIntensityRef.current,
        delta * 2
      )
    }
  })
}
```

From 09-CONTEXT.md D-07 through D-12:
- D-07: Trust -> bridge emissive glow intensity (amber high, dark low) + point lights
- D-08: Courage -> beacon spotlight on/off, rotation speed, light intensity proportional
- D-09: Solidarity -> village window emissive opacity scales with value
- D-10: Awareness -> fog density via FogExp2 (inverted: high awareness = clear)
- D-11: Continuous smooth interpolation 0-100, lerp between states
- D-12: Threshold events: below ~20 triggers dramatic negative, above ~85 triggers positive
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lerp interpolation and threshold events to all landmarks</name>
  <files>src/components/KingdomScene.jsx</files>
  <read_first>
    - src/components/KingdomScene.jsx (current state from Plan 01 — see actual mesh/material names, current component structure)
    - .planning/phases/09-three-js-host-scene/09-RESEARCH.md (Pattern 3: per-frame lerp; Pattern 5: threshold refs; anti-patterns)
    - .planning/phases/09-three-js-host-scene/09-CONTEXT.md (D-07 through D-12 — exact meter-driven visual specs)
    - src/components/CityScene.jsx (reference for useFrame animation patterns — lighthouse beam rotation, window pulse)
  </read_first>
  <action>
Modify each landmark sub-component in KingdomScene.jsx to use the useRef + useEffect + useFrame lerp pattern. CRITICAL: Never call setState inside useFrame — this is the key to THREE-07 (60fps).

**Bridge({ trust }) — per D-07:**

1. Add `useRef` for the cloned bridge material: `const matRef = useRef()`
2. Add `const targetEmissiveRef = useRef(trust / 100)` for lerp target
3. Add `useEffect(() => { targetEmissiveRef.current = trust / 100 }, [trust])`
4. Add `useFrame((_, delta) => { if (matRef.current) { matRef.current.emissiveIntensity = THREE.MathUtils.lerp(matRef.current.emissiveIntensity, targetEmissiveRef.current * 2, delta * 1.5) } })`
5. Set material emissive color to `new THREE.Color('#f59e0b')` (amber) in the useMemo clone
6. Add 3 point lights along the bridge: positions at [-2, 0.5, 0], [0, 0.5, 0], [2, 0.5, 0] (relative to bridge group), color `#f59e0b`, intensity lerped with trust, distance 3. Use refs for point light intensity and lerp them alongside the material.
7. **Threshold below 20:** Bridge point lights go fully dark (target intensity 0), material emissive shifts to red `#ef4444` (change emissive color in useFrame when trust < 20). Add a `bridgeCollapsedRef = useRef(false)` to track threshold state.
8. **Threshold above 85:** Bridge emissive intensity target bumps to 3.0 (extra bright), point lights intensify.

**BeaconTower({ courage }) — per D-08:**

1. Add spotlight ref: `const spotRef = useRef()`
2. Add `const targetIntensityRef = useRef(courage / 100 * 50)` and `const targetRotSpeedRef = useRef(0.3 + (courage / 100) * 0.7)`
3. Add `useEffect` updating both target refs when courage changes
4. Add `useFrame((state, delta) => { ... })`:
   - Lerp spotlight intensity: `spotRef.current.intensity = THREE.MathUtils.lerp(spotRef.current.intensity, targetIntensityRef.current, delta * 2)`
   - Rotate beacon group: `beamGroupRef.current.rotation.y += delta * currentRotSpeed` where currentRotSpeed lerps toward targetRotSpeedRef
5. Spotlight props: color `#f5c542`, angle 0.15, penumbra 0.5, distance 25
6. Add a point light on the beacon top (warm glow): color `#f5c542`, intensity lerped with courage
7. **Threshold below 20:** Spotlight intensity target drops to 0, rotation speed drops to 0. Beacon goes completely dark. Track with `beaconDarkRef = useRef(false)`.
8. **Threshold above 85:** Full beacon — intensity target 80, rotation speed 1.5, add a warm ambient glow from beacon position (or increase existing point light radius).

**VillageQuarter({ solidarity }) — per D-09:**

1. Clone the village material(s) in useMemo. Set emissive to `new THREE.Color('#f5c542')` (warm window glow).
2. Add `const targetOpacityRef = useRef(solidarity / 100)`
3. Add `useEffect` updating target when solidarity changes
4. Add `useFrame` lerping `matRef.current.emissiveIntensity` toward `targetOpacityRef.current * 1.5`
5. If using multiple house models in a group, each house can share the same material ref or have individual refs (sharing is fine since they all respond to the same solidarity value).
6. Add 2-3 small point lights among village buildings: color `#f5c542`, intensity lerped with solidarity, distance 2-3.
7. **Threshold below 20:** Rolling blackout effect — village emissive target goes to 0, point lights go dark. Per CLAUDE.md: "buildings go dark in a wave, left to right." Implement by staggering the lerp targets: first house material target set to 0 immediately, second after 0.5s, third after 1.0s (use `setTimeout` in the useEffect that detects threshold, updating individual refs).
8. **Threshold above 85:** Extra warm — emissive intensity target 2.5, all point lights bright.

**FogController({ awareness }) — per D-10:**

This component already uses useEffect (no useFrame needed — fog density is set discretely, not every frame). Keep the existing pattern:
- `density = 0.015 + (1 - awareness / 100) * 0.04` (inverted per D-10)
- `scene.fog = new THREE.FogExp2('#0a0a14', density)`
- **Threshold above 85:** Density drops to near zero (0.002). Optionally add a subtle warm directional light to simulate dawn glow — `new THREE.DirectionalLight('#f5c542', 0.15)` added to the scene. Track with `sunriseRef`.
- **Threshold below 20:** Density maxes at 0.06 — heavy fog obscuring lower third of scene.

**General implementation rules:**
- Every `useFrame` callback must ONLY read refs — never call `useState` setter
- Every `useEffect` that updates a ref should have the meter value in its dependency array
- Material clones happen once in `useMemo` with `[materials]` dependency — NOT per frame
- Threshold refs (`bridgeCollapsedRef`, `beaconDarkRef`, etc.) reset to false when meter recovers above the threshold value
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && grep -c "MathUtils.lerp" src/components/KingdomScene.jsx && grep -c "useFrame" src/components/KingdomScene.jsx && grep -c "useRef" src/components/KingdomScene.jsx && grep -c "targetEmissiveRef\|targetIntensityRef\|targetOpacityRef\|targetRotSpeed" src/components/KingdomScene.jsx && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/KingdomScene.jsx contains at least 4 `THREE.MathUtils.lerp` calls (bridge, beacon intensity, beacon rotation, village)
    - src/components/KingdomScene.jsx contains at least 4 `useFrame` calls (one per animated landmark)
    - src/components/KingdomScene.jsx contains `targetEmissiveRef` (bridge), `targetIntensityRef` (beacon), `targetOpacityRef` or similar (village)
    - src/components/KingdomScene.jsx does NOT contain `setState` or `set[A-Z]` calls inside any useFrame callback (verify with grep: `grep -A 5 "useFrame" src/components/KingdomScene.jsx` should show no setState)
    - Bridge component has threshold logic: grep for `trust < 20` or `trust <= 20` and `#ef4444` (red emissive on collapse)
    - BeaconTower component has threshold logic: grep for `courage < 20` with intensity targeting 0
    - VillageQuarter component has threshold logic: grep for `solidarity < 20` with blackout behavior
    - FogController has threshold logic: grep for `awareness > 85` or `awareness >= 85`
    - Material clones use `useMemo`: grep for `useMemo.*clone` returns at least 2 matches
    - `npx vite build` completes without errors
  </acceptance_criteria>
  <done>All 4 landmarks smoothly interpolate between states using per-frame lerp. Threshold events fire at extremes (below 20, above 85). No setState in useFrame. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual + performance verification</name>
  <files>src/components/KingdomScene.jsx</files>
  <action>
Human visually verifies the complete 3D scene on the host screen. No code changes — this is a verification checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>Human has confirmed: 3D scene renders with 4 landmarks, smooth lerp transitions visible on world state change, threshold events trigger at extremes, 60fps maintained, player phone does not load Three.js.</done>
  <what-built>
    KingdomScene.jsx: a live 3D nighttime kingdom scene with 4 GLTF landmarks (bridge, beacon tower, village cluster, fog terrain) wired to world state via smooth lerp interpolation and dramatic threshold events. Lazy-loaded in Host.jsx with CityPlaceholder as Suspense fallback. KingdomMap.jsx deleted.
  </what-built>
  <how-to-verify>
    1. Run `npx vite --open` — the app opens in browser
    2. Create a session (host flow) — the host screen should show the 3D kingdom scene instead of the old SVG KingdomMap
    3. Verify 4 landmarks are visible: a bridge, a beacon/tower, village buildings, and fog/terrain
    4. The scene should have: nighttime atmosphere, amber lighting, stars, slow camera rotation
    5. Open browser DevTools -> Performance tab -> record 5 seconds of the scene -> verify frame rate is near 60fps (no sustained drops below 45fps)
    6. If possible, start a round and submit choices to trigger a world state change — landmarks should visibly shift (bridge glow, beacon brightness, village window warmth, fog density)
    7. To test threshold events: manually edit a world state value in Supabase dashboard to below 20 (e.g. trust=15) and verify the bridge goes dark/red; set awareness to 90 and verify fog nearly clears
    8. Verify on the player phone view (join as a player on a second device or tab): the 3D scene should NOT appear; only the existing CSS meter bars should show. Check DevTools Network tab — no three.js chunks should load on the player page.
  </how-to-verify>
  <resume-signal>Type "approved" if the scene looks good and performs well, or describe any visual/performance issues to address.</resume-signal>
</task>

</tasks>

<verification>
1. `npx vite build` succeeds with no errors
2. `grep -c "MathUtils.lerp" src/components/KingdomScene.jsx` returns 4+
3. `grep -c "useFrame" src/components/KingdomScene.jsx` returns 4+
4. `grep "setState\|set[A-Z]" src/components/KingdomScene.jsx` returns 0 matches inside useFrame blocks
5. `grep "trust < 20\|courage < 20\|solidarity < 20\|awareness > 85" src/components/KingdomScene.jsx` returns 4+ threshold checks
6. Visual: host screen shows reactive 3D scene at 60fps
7. Visual: player phone does NOT load Three.js
</verification>

<success_criteria>
- All 4 landmarks animate smoothly via lerp when world state changes (not instant jumps)
- Threshold events are visually distinct at extreme values (below 20 and above 85)
- Scene maintains 60fps on a standard laptop during active Supabase subscription events
- No setState calls inside useFrame — all animation driven by refs
- Human has verified the visual quality and performance on host screen
</success_criteria>

<output>
After completion, create `.planning/phases/09-three-js-host-scene/09-02-SUMMARY.md`
</output>
