---
phase: 06-kingdom-ui-overhaul
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/pages/Host.jsx
  - src/pages/Host.module.css
  - src/components/WorldStatePanel.jsx
  - src/components/ConsequenceReveal.jsx
  - src/components/CityScene.jsx
autonomous: true
requirements: [UI-05, UI-07, UI-08]

must_haves:
  truths:
    - "Host screen shows KingdomMap instead of CityScene/CityPlaceholder in all 3 views (lobby, round, end)"
    - "All MeterBar labels across the entire app display landmark names (Bridge of Accord, Citadel Beacon, Village Quarter, Fog of the Vale) instead of Trust/Courage/Solidarity/Awareness"
    - "Host round eyebrow shows 'The Council Deliberates' when active, 'The Realm Has Spoken' when round closed"
    - "KingdomMap landmark tiers visually update after each round based on worldState values"
  artifacts:
    - path: "src/pages/Host.jsx"
      provides: "KingdomMap integration replacing CityScene, updated meter labels"
      contains: "KingdomMap"
    - path: "src/components/WorldStatePanel.jsx"
      provides: "Landmark-named meter labels"
      contains: "Bridge of Accord"
    - path: "src/components/ConsequenceReveal.jsx"
      provides: "Landmark-named meter labels"
      contains: "Citadel Beacon"
  key_links:
    - from: "src/pages/Host.jsx"
      to: "src/components/KingdomMap.jsx"
      via: "import and render with worldState prop"
      pattern: "import KingdomMap"
    - from: "src/pages/Host.jsx"
      to: "MeterBar label props"
      via: "label prop strings at all call sites"
      pattern: "Bridge of Accord"
---

<objective>
Wire KingdomMap into Host.jsx (replacing CityScene), update all MeterBar labels across the app to landmark names, update host eyebrow copy, and deprecate CityScene.

Purpose: Complete the host screen kingdom visual (UI-05) and ensure landmark names are consistent everywhere (UI-07, UI-08).
Output: Host.jsx renders KingdomMap in all views, all meter labels use landmark names, CityScene import removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-kingdom-ui-overhaul/06-CONTEXT.md
@.planning/phases/06-kingdom-ui-overhaul/06-UI-SPEC.md
@.planning/phases/06-kingdom-ui-overhaul/06-01-SUMMARY.md

<interfaces>
<!-- KingdomMap component created in Plan 01 -->
From src/components/KingdomMap.jsx:
```javascript
export default function KingdomMap({ worldState })
// worldState: { trust, courage, solidarity, awareness } (0-100 each)
// Renders full SVG kingdom map with tier-reactive landmarks
```

<!-- Host.jsx CityScene render sites (3 locations to replace) -->
From src/pages/Host.jsx:
```javascript
// Line 14: const CityScene = lazy(() => import('../components/CityScene.jsx'))
// Line 367-369: <Suspense fallback={null}><CityScene worldState={worldState} /></Suspense>  (end view)
// Line 434-436: <Suspense fallback={null}><CityScene worldState={worldState} /></Suspense>  (round view)
// Line 522-524: <Suspense fallback={null}><CityScene worldState={worldState} /></Suspense>  (lobby view)
```

<!-- MeterBar call sites to update labels -->
All files with MeterBar label="Trust" (etc.):
- src/pages/Host.jsx: lines ~412-415 (end meters), ~479-482 (round meters)
- src/components/WorldStatePanel.jsx: lines ~46-49
- src/components/ConsequenceReveal.jsx: lines ~18-21
- src/pages/Play.jsx: lines ~431-434 (pass consequence), ~505-508 (missed submit)
  NOTE: Play.jsx meter labels are handled here too for completeness.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Host.jsx — replace CityScene with KingdomMap + update eyebrow copy</name>
  <files>src/pages/Host.jsx, src/pages/Host.module.css</files>
  <read_first>
    - src/pages/Host.jsx (full current source — all 3 CityScene render sites, meter label call sites, eyebrow text)
    - src/pages/Host.module.css (current styles)
    - src/components/KingdomMap.jsx (Plan 01 output — verify export and props)
    - .planning/phases/06-kingdom-ui-overhaul/06-UI-SPEC.md (Host.jsx section, eyebrow copy, map placement)
  </read_first>
  <action>
**Host.jsx import changes:**

1. Remove lazy CityScene import: delete `const CityScene = lazy(() => import('../components/CityScene.jsx'))`
2. Remove `lazy, Suspense` from the React import (keep `useState, useEffect, useReducer, useRef`)
3. Add: `import KingdomMap from '../components/KingdomMap.jsx'`

**Host.jsx render changes — replace all 3 CityScene sites:**

4. **End view** (~line 366-369): Replace the entire `<div className={styles.canvas}><Suspense fallback={null}><CityScene worldState={worldState} /></Suspense></div>` with `<div className={styles.canvas}><KingdomMap worldState={worldState} /></div>`

5. **Round view** (~line 433-437): Same replacement — `<div className={styles.canvas}><KingdomMap worldState={worldState} /></div>`

6. **Lobby view** (~line 519-524): Same replacement — `<div className={styles.canvas}><KingdomMap worldState={worldState} /></div>`

**Host.jsx eyebrow copy updates:**

7. Round view eyebrow (~line 443): Change `{roundState.roundClosed ? 'DELIBERATION CLOSED' : 'COUNCIL IN SESSION'}` to `{roundState.roundClosed ? 'The Realm Has Spoken' : 'The Council Deliberates'}`

**Host.jsx meter label updates (all sites):**

8. End view meters (~lines 412-415): Change all 4 MeterBar label props:
   - `label="Trust"` → `label="Bridge of Accord"`
   - `label="Courage"` → `label="Citadel Beacon"`
   - `label="Solidarity"` → `label="Village Quarter"`
   - `label="Awareness"` → `label="Fog of the Vale"`

9. Round view meters (~lines 479-482): Same 4 label replacements.

**Host.jsx lobby round selector removal:**

10. Remove the `.roundSelector` div from the lobby view (~lines 536-551) — this is a duplicate of HostSetup's selector that was already in the lobby. Delete the entire block:
```jsx
<div className={styles.roundSelector}>
  <span className={styles.roundLabel}>Rounds</span>
  {[3, 4, 5, 6].map(n => (...))}
</div>
```

11. Update the `startGame()` function (~line 253): change `total_rounds: totalRounds` to `total_rounds: session.total_rounds` (use the value already set in the DB from Landing.jsx).

12. The `totalRounds` state and `setTotalRounds` can be removed since it's no longer needed. Replace `session.current_round >= session.total_rounds` check on `isLastRound` (already using session.total_rounds) — verify this is already correct.

**Host.module.css changes:**

13. Remove `.roundSelector`, `.roundLabel`, `.roundBtn`, `.roundBtnActive`, `.roundBtnInactive` if they exist in Host.module.css.
  </action>
  <verify>
    <automated>grep "KingdomMap" src/pages/Host.jsx && grep -c "CityScene" src/pages/Host.jsx | grep "^0$" && grep "Bridge of Accord" src/pages/Host.jsx && grep "The Realm Has Spoken" src/pages/Host.jsx && grep "The Council Deliberates" src/pages/Host.jsx && grep -c "roundSelector" src/pages/Host.jsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Host.jsx contains `import KingdomMap from '../components/KingdomMap.jsx'`
    - src/pages/Host.jsx does NOT contain `CityScene` (no import, no render)
    - src/pages/Host.jsx does NOT contain `Suspense` import
    - src/pages/Host.jsx contains `<KingdomMap worldState={worldState} />` (3 occurrences)
    - src/pages/Host.jsx contains `label="Bridge of Accord"` (at least 2 occurrences)
    - src/pages/Host.jsx contains `label="Citadel Beacon"` (at least 2)
    - src/pages/Host.jsx contains `label="Village Quarter"` (at least 2)
    - src/pages/Host.jsx contains `label="Fog of the Vale"` (at least 2)
    - src/pages/Host.jsx contains `The Realm Has Spoken`
    - src/pages/Host.jsx contains `The Council Deliberates`
    - src/pages/Host.jsx does NOT contain `roundSelector`
    - src/pages/Host.jsx does NOT contain `setTotalRounds`
  </acceptance_criteria>
  <done>Host.jsx renders KingdomMap in all 3 views (lobby, round, end), all meter labels use landmark names, eyebrow shows kingdom copy, round selector removed from lobby.</done>
</task>

<task type="auto">
  <name>Task 2: Update MeterBar labels in WorldStatePanel, ConsequenceReveal, and Play.jsx</name>
  <files>src/components/WorldStatePanel.jsx, src/components/ConsequenceReveal.jsx, src/pages/Play.jsx</files>
  <read_first>
    - src/components/WorldStatePanel.jsx (full source — 4 MeterBar calls at lines 46-49)
    - src/components/ConsequenceReveal.jsx (full source — 4 MeterBar calls at lines 18-21)
    - src/pages/Play.jsx (full source — MeterBar calls in pass consequence ~431-434 and missed submit ~505-508)
    - .planning/phases/06-kingdom-ui-overhaul/06-UI-SPEC.md (MeterBar label update section, D-07)
  </read_first>
  <action>
**In all 3 files, replace MeterBar label props with landmark names:**

The mapping (per D-07):
- `label="Trust"` → `label="Bridge of Accord"`
- `label="Courage"` → `label="Citadel Beacon"`
- `label="Solidarity"` → `label="Village Quarter"`
- `label="Awareness"` → `label="Fog of the Vale"`

**WorldStatePanel.jsx** (~lines 46-49): Update all 4 MeterBar label props. Also update the section label text from `"World State"` to `"The Realm"` (~line 44).

**ConsequenceReveal.jsx** (~lines 18-21): Update all 4 MeterBar label props. Also update the meters label from `"WORLD STATE"` to `"THE REALM"` (~line 17).

**Play.jsx** — Update MeterBar labels in these locations:
1. Pass consequence meters (~lines 431-434): 4 label prop updates
2. Missed submit meters (~lines 505-508): 4 label prop updates

Note: Play.jsx may have already had "WORLD STATE" labels updated to "THE REALM" in Plan 02. If so, leave them. If not, update the `metersLabel` text in both pass consequence and missed submit views from `"WORLD STATE"` to `"THE REALM"`.
  </action>
  <verify>
    <automated>grep "Bridge of Accord" src/components/WorldStatePanel.jsx && grep "Citadel Beacon" src/components/ConsequenceReveal.jsx && grep "Village Quarter" src/pages/Play.jsx && grep "Fog of the Vale" src/components/WorldStatePanel.jsx && grep -c 'label="Trust"' src/components/WorldStatePanel.jsx src/components/ConsequenceReveal.jsx src/pages/Play.jsx | grep ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - src/components/WorldStatePanel.jsx contains `label="Bridge of Accord"`
    - src/components/WorldStatePanel.jsx contains `label="Citadel Beacon"`
    - src/components/WorldStatePanel.jsx contains `label="Village Quarter"`
    - src/components/WorldStatePanel.jsx contains `label="Fog of the Vale"`
    - src/components/WorldStatePanel.jsx does NOT contain `label="Trust"`
    - src/components/WorldStatePanel.jsx contains `"The Realm"` (section label)
    - src/components/ConsequenceReveal.jsx contains `label="Bridge of Accord"`
    - src/components/ConsequenceReveal.jsx does NOT contain `label="Trust"`
    - src/components/ConsequenceReveal.jsx contains `THE REALM`
    - src/pages/Play.jsx contains `label="Bridge of Accord"` (in meter sections)
    - src/pages/Play.jsx does NOT contain `label="Trust"` anywhere
  </acceptance_criteria>
  <done>All MeterBar call sites across the entire app use landmark names. No remaining "Trust"/"Courage"/"Solidarity"/"Awareness" labels on meters.</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors
2. `grep -r 'label="Trust"' src/` returns zero results
3. `grep -r 'CityScene' src/pages/Host.jsx` returns zero results
4. KingdomMap renders in all 3 Host views with correct worldState
5. All meter labels show landmark names consistently
</verification>

<success_criteria>
- Host.jsx imports and renders KingdomMap (not CityScene) in lobby, round, and end views
- Zero remaining `label="Trust"` / `label="Courage"` / `label="Solidarity"` / `label="Awareness"` in any file
- Host eyebrow shows "The Council Deliberates" / "The Realm Has Spoken"
- Lobby round selector removed from Host.jsx
- vite build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-kingdom-ui-overhaul/06-03-SUMMARY.md`
</output>
