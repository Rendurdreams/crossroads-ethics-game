---
phase: 20
plan: "20-02"
title: "Host projector declutter and phone cue card restructure"
wave: 1
depends_on: []
files_modified:
  - src/pages/Host.jsx
  - src/pages/HostRemote.jsx
  - src/pages/HostRemote.module.css
autonomous: true
---

# Host Projector Declutter and Phone Cue Card Restructure

<objective>
Remove discussion prompts and host notes from the Host.jsx projector lesson overlay, and restructure the HostRemote.jsx phone view to show a combined facilitator cue card with ethics cheat sheet and discussion prompts derived from scenario data.
</objective>

<tasks>

<task id="T1" title="Remove discussionPrompts and hostNotes from Host.jsx lesson overlay">
<read_first>
- src/pages/Host.jsx
</read_first>
<action>
In `src/pages/Host.jsx`, delete the two blocks in the lesson overlay that render discussion prompts and host notes. Specifically remove:

1. The discussion prompts block (approximately lines 872-879):
```jsx
{currentScenario?.discussionPrompts?.length > 0 && (
  <div className={styles.discussionPrompts}>
    <p className={styles.discussionLabel}>DISCUSSION</p>
    {currentScenario.discussionPrompts.map((prompt, i) => (
      <p key={i} className={styles.discussionItem}>{prompt}</p>
    ))}
  </div>
)}
```

2. The host notes block (approximately lines 882-889):
```jsx
{currentScenario?.hostNotes?.length > 0 && (
  <details className={styles.hostNotes}>
    <summary className={styles.hostNotesLabel}>HOST NOTES</summary>
    {currentScenario.hostNotes.map((note, i) => (
      <p key={i} className={styles.hostNoteItem}>{note}</p>
    ))}
  </details>
)}
```

Leave the `lessonResearch` / `HowOthersChose` block that follows these — it stays on the projector. Leave the CSS classes in Host.module.css (harmless dead styles, cleaning optional).
</action>
<acceptance_criteria>
- `Host.jsx` does NOT contain the string `discussionPrompts.map`
- `Host.jsx` does NOT contain the string `hostNotes.map`
- `Host.jsx` does NOT contain the string `HOST NOTES` inside the lesson overlay (search between `showLesson` and `lessonResearch`)
- `Host.jsx` still contains `HowOthersChose` component rendering
- `Host.jsx` still contains `lessonTitle` and `lessonBody` rendering
- `Host.jsx` still contains `frameworkCallout` rendering
- `Host.jsx` still contains `spotlightCallout` / `conflictSpotlight` rendering
</acceptance_criteria>
</task>

<task id="T2" title="Restructure HostRemote.jsx notes section as facilitator cue card">
<read_first>
- src/pages/HostRemote.jsx
- src/pages/HostRemote.module.css
- src/lib/frameworks.js
</read_first>
<action>
In `src/pages/HostRemote.jsx`:

1. Add import at top: `import { FRAMEWORKS } from '../lib/frameworks.js'` (if not already imported)

2. Replace the existing host notes + discussion prompts render block (lines ~449-466) with a unified facilitator cue card:

```jsx
{/* Facilitator cue card */}
{currentScenario && (
  <div className={styles.cueCard}>
    <p className={styles.cueHeader}>ETHICS CUE CARD</p>
    
    {/* Frameworks in play this round — computed from choices */}
    <div className={styles.cueSection}>
      <p className={styles.cueLabel}>FRAMEWORKS IN PLAY</p>
      <p className={styles.cueText}>
        {[...new Set(currentScenario.choices.flatMap(c => c.frameworks))]
          .map(f => FRAMEWORKS[f]?.name || f)
          .join(' \u2022 ')}
      </p>
    </div>

    {/* Moral tension */}
    {currentScenario.moralTension && (
      <div className={styles.cueSection}>
        <p className={styles.cueLabel}>MORAL TENSION</p>
        <p className={styles.cueText}>{currentScenario.moralTension}</p>
      </div>
    )}

    {/* Host notes — ethics cheat sheet bullets */}
    {currentScenario.hostNotes?.length > 0 && (
      <div className={styles.cueSection}>
        <p className={styles.cueLabel}>SAY THIS</p>
        {currentScenario.hostNotes.map((note, i) => (
          <p key={i} className={styles.cueBullet}>{note}</p>
        ))}
      </div>
    )}

    {/* Discussion prompts */}
    {currentScenario.discussionPrompts?.length > 0 && (
      <div className={styles.cueSection}>
        <p className={styles.cueLabel}>ASK THE CLASS</p>
        {currentScenario.discussionPrompts.map((prompt, i) => (
          <p key={i} className={styles.cueBullet}>{prompt}</p>
        ))}
      </div>
    )}
  </div>
)}
```

This replaces the old two-block layout with a single unified cue card. The "FRAMEWORKS IN PLAY" section is computed at runtime from the scenario choices — no new data field needed.
</action>
<acceptance_criteria>
- `HostRemote.jsx` contains the string `ETHICS CUE CARD`
- `HostRemote.jsx` contains the string `FRAMEWORKS IN PLAY`
- `HostRemote.jsx` contains the string `MORAL TENSION`
- `HostRemote.jsx` contains the string `SAY THIS`
- `HostRemote.jsx` contains the string `ASK THE CLASS`
- `HostRemote.jsx` contains `currentScenario.choices.flatMap(c => c.frameworks)`
- `HostRemote.jsx` imports `FRAMEWORKS` from frameworks.js
- `HostRemote.jsx` does NOT contain the old standalone `HOST NOTES` label (the `<p className={styles.notesLabel}>HOST NOTES</p>` pattern)
</acceptance_criteria>
</task>

<task id="T3" title="Add cue card CSS styles to HostRemote.module.css">
<read_first>
- src/pages/HostRemote.module.css
</read_first>
<action>
Add the following styles to the end of `src/pages/HostRemote.module.css`:

```css
/* ── Facilitator cue card ── */
.cueCard {
  background: rgba(59, 130, 246, 0.06);
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 8px;
  padding: 12px;
  margin-top: 12px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.cueHeader {
  font-family: var(--mono);
  font-size: 10px;
  font-weight: 700;
  color: var(--accent-blue);
  letter-spacing: 0.12em;
  text-transform: uppercase;
  border-bottom: 1px solid rgba(59, 130, 246, 0.15);
  padding-bottom: 6px;
}

.cueSection {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.cueLabel {
  font-family: var(--mono);
  font-size: 9px;
  font-weight: 600;
  color: rgba(59, 130, 246, 0.7);
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.cueText {
  font-size: 13px;
  color: var(--text-primary);
  line-height: 1.5;
}

.cueBullet {
  font-size: 12px;
  color: var(--text-dim);
  line-height: 1.5;
  padding-left: 10px;
  border-left: 2px solid rgba(59, 130, 246, 0.2);
}
```
</action>
<acceptance_criteria>
- `HostRemote.module.css` contains `.cueCard`
- `HostRemote.module.css` contains `.cueHeader`
- `HostRemote.module.css` contains `.cueSection`
- `HostRemote.module.css` contains `.cueLabel`
- `HostRemote.module.css` contains `.cueText`
- `HostRemote.module.css` contains `.cueBullet`
- `.cueCard` has `background: rgba(59, 130, 246, 0.06)` matching the existing glass style
</acceptance_criteria>
</task>

</tasks>

<verification>
- Run `npx vite build` — no build errors
- Host projector lesson overlay shows: moral tension, teaches, framework callout, spotlight, HowOthersChose — but NO discussion prompts block and NO host notes collapsible
- HostRemote phone view shows a unified "ETHICS CUE CARD" with: frameworks in play (computed from choices), moral tension, host notes (when available), discussion prompts (when available)
- When playing kingdom-arc (which currently has no hostNotes/discussionPrompts), the cue card still shows FRAMEWORKS IN PLAY and MORAL TENSION sections — the SAY THIS and ASK THE CLASS sections are hidden (null-safe)
</verification>

<must_haves>
- Host projector lesson overlay does NOT show discussion prompts or host notes
- Host projector lesson overlay still shows vote tally, lesson content, framework callout, conflict spotlight, and HowOthersChose
- HostRemote phone view shows a unified facilitator cue card with ethics cheat sheet sections
- The cue card computes "frameworks in play" from the scenario choices at runtime
- All rendering is null-safe — kingdom-arc works without hostNotes/discussionPrompts fields
</must_haves>
