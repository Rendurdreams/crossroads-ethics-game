---
phase: 15-divided-kingdom-phase-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ScenarioCard.jsx
  - src/components/ScenarioCard.module.css
  - src/components/ConsequenceReveal.jsx
  - src/components/ConsequenceReveal.module.css
  - src/lib/howOthersChose.js
autonomous: true
requirements:
  - DK2-05
  - DK2-06
  - DK2-07
must_haves:
  truths:
    - "ScenarioCard renders 4 choice buttons when a scenario has 4 choices"
    - "Roman numeral IV appears for 4th choice button"
    - "ConsequenceReveal displays conscienceLayer text when present on the choice"
    - "How Others Chose reference data is available as an importable module"
  artifacts:
    - path: "src/components/ScenarioCard.jsx"
      provides: "4-choice support with ROMAN IV"
      contains: "IV"
    - path: "src/components/ConsequenceReveal.jsx"
      provides: "Conscience layer display"
      contains: "conscienceLayer"
    - path: "src/lib/howOthersChose.js"
      provides: "Reference percentages per round from PRD"
      contains: "HOW_OTHERS_CHOSE"
  key_links:
    - from: "src/components/ConsequenceReveal.jsx"
      to: "src/lib/scenarios/packs/kingdom-arc.js"
      via: "conscienceLayer prop passed from Play.jsx"
      pattern: "conscienceLayer"
    - from: "src/lib/howOthersChose.js"
      to: "src/pages/Play.jsx"
      via: "import and render in post-round screen"
      pattern: "HOW_OTHERS_CHOSE"
---

<objective>
Update UI components for Phase 2 features: ScenarioCard handles 4 choices, ConsequenceReveal shows conscience layer text, and create How Others Chose reference data module.

Purpose: Per D-06 (R7 4th choice needs UI support), D-07 (conscience layer display), D-04 (How Others Chose reference data).
Output: Updated ScenarioCard, ConsequenceReveal, new howOthersChose.js data module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-divided-kingdom-phase-2/15-PRD.md
@src/components/ScenarioCard.jsx
@src/components/ScenarioCard.module.css
@src/components/ConsequenceReveal.jsx
@src/components/ConsequenceReveal.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScenarioCard 4-choice support + ConsequenceReveal conscience layer</name>
  <files>src/components/ScenarioCard.jsx, src/components/ScenarioCard.module.css, src/components/ConsequenceReveal.jsx, src/components/ConsequenceReveal.module.css</files>
  <read_first>src/components/ScenarioCard.jsx, src/components/ScenarioCard.module.css, src/components/ConsequenceReveal.jsx, src/components/ConsequenceReveal.module.css</read_first>
  <action>
**Part A — ScenarioCard 4-choice support (per D-06):**

1. Change the ROMAN array from `['I', 'II', 'III']` to `['I', 'II', 'III', 'IV']`

2. The component already maps over `scenario.choices` dynamically and uses `ROMAN[choice.choiceIndex]` — so the 4th button will render automatically once ROMAN has the IV entry. No other JSX changes needed.

3. In ScenarioCard.module.css, check if `.choices` has any grid or layout that assumes max 3 items. If it uses flex-direction: column, no change needed. If it uses a grid with fixed columns, adjust to accommodate 4 items. The buttons should stack vertically on mobile.

**Part B — ConsequenceReveal conscience layer (per D-07):**

1. Add a `conscienceLayer` prop to ConsequenceReveal component signature:
   ```javascript
   export default function ConsequenceReveal({ consequence, conscienceLayer, framework, worldState, moralValues, moralStances, hasMoralConflict })
   ```

2. Display the conscience layer text AFTER the consequence text and BEFORE the framework section. Add it as a distinct visual element — a shorter, punchier line that carries the moral weight:
   ```jsx
   {conscienceLayer && (
     <p className={styles.conscienceLayer}>{conscienceLayer}</p>
   )}
   ```

3. Add `.conscienceLayer` style in ConsequenceReveal.module.css:
   ```css
   .conscienceLayer {
     font-style: italic;
     color: var(--amber, #f5a623);
     font-size: 0.95rem;
     line-height: 1.5;
     margin-top: 12px;
     padding-top: 12px;
     border-top: 1px solid rgba(245, 158, 11, 0.2);
   }
   ```
   The amber color and italic style distinguish it from the main consequence narrative. It should feel like an inner voice — personal, weighted, second-person.
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && grep "'IV'" src/components/ScenarioCard.jsx && grep "conscienceLayer" src/components/ConsequenceReveal.jsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'IV'" src/components/ScenarioCard.jsx` matches (ROMAN array has IV)
    - `grep "conscienceLayer" src/components/ConsequenceReveal.jsx` matches at least twice (prop + render)
    - `grep "conscienceLayer" src/components/ConsequenceReveal.module.css` matches (style exists)
    - ScenarioCard still renders correctly for 3-choice scenarios (no regression)
  </acceptance_criteria>
  <done>ScenarioCard renders 4 choices with Roman numeral IV, ConsequenceReveal displays conscience layer text in amber italic</done>
</task>

<task type="auto">
  <name>Task 2: Create How Others Chose reference data module</name>
  <files>src/lib/howOthersChose.js</files>
  <read_first>.planning/phases/15-divided-kingdom-phase-2/15-PRD.md</read_first>
  <action>
Create a new data module `src/lib/howOthersChose.js` containing the hardcoded reference percentages from the PRD (per D-04). This is research-baseline data from Awad et al. [3] / MIT Moral Machine pattern.

The module exports a lookup function and a data constant:

```javascript
/**
 * "How Others Chose" — reference percentages per round from research baseline.
 * Awad et al. (2018) — MIT Moral Machine pattern [3].
 * Used alongside live class percentages after each round.
 */
export const HOW_OTHERS_CHOSE = {
  'round-1': [
    { choiceIndex: 0, label: 'Share Equally', pct: 41 },
    { choiceIndex: 1, label: 'Protect the Core', pct: 28 },
    { choiceIndex: 2, label: 'Triage', pct: 31 }
  ],
  'round-2': [
    { choiceIndex: 0, label: 'Iron Protocol', pct: 38 },
    { choiceIndex: 1, label: 'Open Flame', pct: 35 },
    { choiceIndex: 2, label: 'Border Only', pct: 27 }
  ],
  'round-3': [
    { choiceIndex: 0, label: 'Grant Citizenship', pct: 52 },
    { choiceIndex: 1, label: 'Deny', pct: 12 },
    { choiceIndex: 2, label: 'Limited Charter', pct: 36 }
  ],
  'round-4': [
    { choiceIndex: 0, label: 'Reveal', pct: 44 },
    { choiceIndex: 1, label: 'Seal', pct: 21 },
    { choiceIndex: 2, label: 'Council of Seven', pct: 35 }
  ],
  'round-5': [
    { choiceIndex: 0, label: 'Open', pct: 49 },
    { choiceIndex: 1, label: 'Keep Sealed', pct: 18 },
    { choiceIndex: 2, label: 'Partial Draw', pct: 33 }
  ],
  'round-6': [
    { choiceIndex: 0, label: 'Free Irel', pct: 61 },
    { choiceIndex: 1, label: 'Maintain Binding', pct: 14 },
    { choiceIndex: 2, label: 'One Year', pct: 25 }
  ],
  'round-7': [
    { choiceIndex: 0, label: 'Retribution', pct: 33 },
    { choiceIndex: 1, label: 'Reconciliation', pct: 28 },
    { choiceIndex: 2, label: 'Truth Commission', pct: 24 },
    { choiceIndex: 3, label: 'Cultural Tribunal', pct: 15 }
  ],
  'round-bombshell': [
    { choiceIndex: 0, label: 'Publish Everything', pct: 55 },
    { choiceIndex: 1, label: 'Bury', pct: 17 },
    { choiceIndex: 2, label: 'Publish After Death', pct: 28 }
  ]
}

/**
 * Get reference percentages for a given scenario ID.
 * @param {string} scenarioId - e.g. 'round-1', 'round-bombshell'
 * @returns {Array<{choiceIndex: number, label: string, pct: number}>|null}
 */
export function getHowOthersChose(scenarioId) {
  return HOW_OTHERS_CHOSE[scenarioId] ?? null
}
```
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && node -e "import('./src/lib/howOthersChose.js').then(m => { console.log('R1:', m.getHowOthersChose('round-1')); console.log('R7 length:', m.getHowOthersChose('round-7').length); console.log('R8:', m.getHowOthersChose('round-bombshell')?.length) })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "HOW_OTHERS_CHOSE" src/lib/howOthersChose.js` matches
    - `grep "getHowOthersChose" src/lib/howOthersChose.js` matches
    - Module exports data for all 8 rounds (round-1 through round-7 + round-bombshell)
    - Round 7 has 4 entries (including Cultural Tribunal at 15%)
    - All percentages per round sum to 100
  </acceptance_criteria>
  <done>How Others Chose reference data available as importable module with all 8 rounds of PRD-specified percentages</done>
</task>

</tasks>

<verification>
- ScenarioCard ROMAN array includes IV
- ConsequenceReveal has conscienceLayer prop and renders it
- howOthersChose.js exports data for all 8 rounds
- R7 reference data has 4 entries
</verification>

<success_criteria>
1. ScenarioCard renders 4 choices correctly
2. ConsequenceReveal shows conscience layer in amber italic when provided
3. How Others Chose reference data module exists with all PRD percentages
4. No regressions in existing 3-choice scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/15-divided-kingdom-phase-2/15-02-SUMMARY.md`
</output>
