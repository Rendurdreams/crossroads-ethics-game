---
phase: 03-game-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MeterBar.jsx
  - src/components/MeterBar.module.css
  - src/components/TimerDisplay.jsx
  - src/components/TimerDisplay.module.css
  - src/components/FrameworkLabel.jsx
  - src/components/FrameworkLabel.module.css
  - src/components/CityPlaceholder.jsx
  - src/components/CityPlaceholder.module.css
  - src/App.jsx
  - src/pages/HostSetup.jsx
  - src/pages/HostSetup.module.css
  - src/pages/Landing.jsx
autonomous: true
requirements:
  - HOST-09
  - PLAY-10

must_haves:
  truths:
    - "MeterBar renders a labeled CSS bar whose fill width matches the value prop (0-100) and transitions smoothly"
    - "MeterBar fill color switches from accent to danger when value < 20"
    - "TimerDisplay shows remaining seconds in large text and a shrinking progress bar"
    - "TimerDisplay color switches from accent to danger when remaining < 10"
    - "FrameworkLabel renders framework name and 1-sentence explanation with fade-in animation"
    - "CityPlaceholder renders a dark reserved panel that looks intentional, not broken"
    - "Landing 'Create Game' redirects to /host-setup/:sessionId instead of /host/:sessionId"
    - "HostSetup page loads session data and renders room code + round selector + Open Lobby button"
  artifacts:
    - path: "src/components/MeterBar.jsx"
      provides: "CSS meter bar component"
      exports: ["default"]
    - path: "src/components/TimerDisplay.jsx"
      provides: "Timer countdown display"
      exports: ["default"]
    - path: "src/components/FrameworkLabel.jsx"
      provides: "Framework label badge"
      exports: ["default"]
    - path: "src/components/CityPlaceholder.jsx"
      provides: "Host left panel placeholder"
      exports: ["default"]
    - path: "src/pages/HostSetup.jsx"
      provides: "Host setup/prep page"
      exports: ["default"]
  key_links:
    - from: "src/App.jsx"
      to: "src/pages/HostSetup.jsx"
      via: "Route path=/host-setup/:sessionId"
      pattern: "host-setup.*HostSetup"
    - from: "src/pages/Landing.jsx"
      to: "/host-setup/:sessionId"
      via: "navigate redirect on create"
      pattern: "navigate.*host-setup"
---

<objective>
Build all shared leaf components (MeterBar, TimerDisplay, FrameworkLabel, CityPlaceholder) and the HostSetup page with routing. These components have no dependencies on each other and are consumed by both the host and player views in Plans 02 and 03.

Purpose: Establish the visual building blocks and routing foundation that Plans 02 and 03 wire into the game loop. Without these, both player and host views would need to inline meter/timer/label rendering.

Output: 4 leaf components with CSS modules, 1 new page (HostSetup), updated App.jsx routing, updated Landing.jsx redirect target.
</objective>

<execution_context>
@/Users/jay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-loop/03-CONTEXT.md
@.planning/phases/03-game-loop/03-UI-SPEC.md

@src/index.css
@src/App.jsx
@src/pages/Landing.jsx
@src/lib/frameworks.js

<interfaces>
<!-- Existing CSS custom properties from src/index.css -->
--bg: #0a0a14
--bg-surface: #12121e
--text: #e5e5e5
--text-h: #f5f5f5
--text-muted: #6b7280
--accent: #f59e0b
--accent-hover: #d97706
--accent-bg: rgba(245, 158, 11, 0.1)
--danger: #ef4444
--serif: Georgia, 'Times New Roman', serif
--sans: 'Inter', system-ui, -apple-system, sans-serif

<!-- From src/lib/frameworks.js — used by FrameworkLabel -->
FRAMEWORKS = {
  consequentialism: { name: 'Consequentialism', description: '...', question: '...' },
  deontology: { name: 'Deontology', description: '...', question: '...' },
  care: { name: 'Care Ethics', description: '...', question: '...' },
  virtue: { name: 'Virtue Ethics', description: '...', question: '...' }
}

<!-- From src/lib/supabase.js — singleton pattern -->
import { supabase } from '../lib/supabase.js'

<!-- CSS Modules pattern established in all pages -->
import styles from './Page.module.css'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared leaf components (MeterBar, TimerDisplay, FrameworkLabel, CityPlaceholder)</name>
  <files>
    src/components/MeterBar.jsx,
    src/components/MeterBar.module.css,
    src/components/TimerDisplay.jsx,
    src/components/TimerDisplay.module.css,
    src/components/FrameworkLabel.jsx,
    src/components/FrameworkLabel.module.css,
    src/components/CityPlaceholder.jsx,
    src/components/CityPlaceholder.module.css
  </files>
  <read_first>
    src/index.css,
    src/lib/frameworks.js,
    src/components/PlayerRoster.jsx,
    src/components/PlayerRoster.module.css,
    .planning/phases/03-game-loop/03-UI-SPEC.md
  </read_first>
  <action>
    Create 4 leaf components. All use CSS Modules pattern (`import styles from './X.module.css'`).

    **MeterBar.jsx** — Props: `label` (string), `value` (number 0-100)
    - Renders a label row: meter name left (14px, weight 600, var(--text-muted)), value number right (14px, weight 600, var(--text-h))
    - Renders a track div: full width, height 8px, background #2e303a, border-radius 4px
    - Renders a fill div inside track: `width: ${value}%`, `transition: width 0.8s ease`, border-radius 4px
    - Fill background: `var(--accent)` when value >= 20, `var(--danger)` when value < 20
    - Apply danger color via inline style or conditional className
    - CSS class for danger transition: `transition: background-color 0.6s ease` on the fill element (per D-20 and animation contract)

    **TimerDisplay.jsx** — Props: `remaining` (number, seconds left), `total` (number, total seconds)
    - "Time remaining" label: 14px var(--text-muted) above the number
    - Large countdown number: 28px, weight 600, formatted as `M:SS` (e.g., "0:42")
    - Color: var(--accent) when remaining >= 10, var(--danger) when remaining < 10 — apply `transition: color 0.2s ease`
    - When remaining === 0, display "Time's up" instead of "0:00"
    - Progress bar below number: 8px height, same color logic as number, border-radius 4px
    - Bar width: `${(remaining / total) * 100}%`, `transition: width 1s linear`
    - Track: same as MeterBar track styling (#2e303a, 8px, border-radius 4px)

    **FrameworkLabel.jsx** — Props: `framework` (string key like 'care'), `explanation` (string, 1-sentence)
    - Import FRAMEWORKS from '../lib/frameworks.js' to resolve display name: `FRAMEWORKS[framework].name`
    - Badge container: background var(--accent-bg), border 1px solid var(--accent), border-radius 4px, padding 8px 12px
    - Framework name: 14px, weight 600, color var(--accent)
    - Explanation text below: 14px, weight 400, color var(--text-muted)
    - Entrance animation via CSS class: opacity 0 to 1, translateY(4px) to translateY(0), duration 300ms ease
    - Animation is triggered by being rendered (use CSS `animation` property with the keyframe, NOT a state toggle)
    - The 400ms delay after consequence is handled by the PARENT (ConsequenceReveal) — FrameworkLabel itself animates immediately on mount

    **CityPlaceholder.jsx** — No props
    - Dark panel: background var(--bg), width 100%, height 100%, display flex, align-items center, justify-content center, flex-direction column
    - Simple inline SVG skyline silhouette: a basic set of rectangles (buildings) in #1a1a2a color, roughly 300px wide, 120px tall. 5-7 rectangles of varying height arranged as a city outline. No external SVG file needed.
    - Below SVG: "CITY VIEW" text in 14px var(--text-muted), uppercase, letter-spacing 0.2em, margin-top 16px
    - Per D-06: must look reserved and intentional, not broken or unfinished
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/MeterBar.jsx contains `export default function MeterBar`
    - src/components/MeterBar.jsx contains `transition: width 0.8s ease` or equivalent in the module.css
    - src/components/MeterBar.module.css contains `var(--accent)` and `var(--danger)`
    - src/components/TimerDisplay.jsx contains `export default function TimerDisplay`
    - src/components/TimerDisplay.jsx contains logic for "Time's up" when remaining === 0
    - src/components/TimerDisplay.module.css contains `transition: width 1s linear`
    - src/components/FrameworkLabel.jsx contains `import { FRAMEWORKS }` from frameworks.js
    - src/components/FrameworkLabel.jsx contains `FRAMEWORKS[framework].name`
    - src/components/FrameworkLabel.module.css contains `translateY(4px)` keyframe animation
    - src/components/CityPlaceholder.jsx contains `CITY VIEW` or `City View` text
    - src/components/CityPlaceholder.jsx contains an inline SVG with rect elements
    - All 4 component files use `import styles from` CSS Modules pattern
    - vite build succeeds with exit code 0
  </acceptance_criteria>
  <done>All 4 leaf components render correctly with proper styling, animations, and color logic. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add HostSetup page, update routing and Landing redirect</name>
  <files>
    src/pages/HostSetup.jsx,
    src/pages/HostSetup.module.css,
    src/App.jsx,
    src/pages/Landing.jsx
  </files>
  <read_first>
    src/App.jsx,
    src/pages/Landing.jsx,
    src/pages/Host.jsx,
    src/pages/Host.module.css,
    .planning/phases/03-game-loop/03-CONTEXT.md,
    .planning/phases/03-game-loop/03-UI-SPEC.md
  </read_first>
  <action>
    Per D-08: Create a new `/host-setup/:sessionId` page that sits between Landing's "Create Game" and the lobby.

    **HostSetup.jsx** — New page at src/pages/HostSetup.jsx
    - useParams() to get sessionId, useNavigate() for navigation
    - On mount: fetch session row from Supabase (`supabase.from('sessions').select('*').eq('id', sessionId).single()`). If not found, navigate to '/'.
    - State: `session` (fetched row), `totalRounds` (default 4), `loading` (boolean)
    - Display heading: "You're the host." (var(--text-h), 28px, weight 600)
    - Display room code: same styling as Host.jsx room code — `font-size: clamp(72px, 12vw, 120px)`, weight 700, letter-spacing 0.15em, color var(--accent)
    - Display "ROOM CODE" label above it: var(--text-muted), 0.8rem, uppercase, letter-spacing 0.2em
    - Round count selector: same as current Host.jsx — buttons for [3, 4, 5, 6], active gets var(--accent) bg, inactive gets var(--bg-surface) with border. Reuse the exact same CSS pattern from Host.module.css.
    - Instructions text: "Share the room code. When your class is ready, open the lobby." — 16px, var(--text-muted), centered, max-width 400px
    - "Open Lobby" button: same styling as startBtn in Host.module.css — full width, var(--accent) bg, 14px padding, 1.1rem font, weight 600, border-radius 8px
    - On "Open Lobby" click: update session in Supabase with `total_rounds: totalRounds`, then navigate to `/host/${sessionId}`
    - Page layout: centered flex column, padding 40px 24px, background var(--bg), min-height 100vh

    **App.jsx** — Add new route
    - Import HostSetup: `import HostSetup from './pages/HostSetup.jsx'`
    - Add Route: `<Route path="/host-setup/:sessionId" element={<HostSetup />} />` before the host route

    **Landing.jsx** — Change redirect target
    - In `createSession()`, change `navigate(\`/host/${result.data.id}\`)` to `navigate(\`/host-setup/${result.data.id}\`)`
    - This is a single line change on line 55

    **HostSetup.module.css** — Create with styles for the setup page
    - .page: min-height 100vh, display flex, flex-direction column, align-items center, padding 40px 24px, background var(--bg)
    - .heading: font-size 28px, weight 600, color var(--text-h), margin-bottom 8px, font-family var(--sans)
    - .roomLabel: same as Host.module.css .roomLabel
    - .roomCode: same as Host.module.css .roomCode
    - .instructions: font-size 16px, color var(--text-muted), text-align center, max-width 400px, margin 24px auto, line-height 1.5
    - .roundSelector, .roundBtn, .roundBtnActive, .roundBtnInactive: copy from Host.module.css
    - .openBtn: copy startBtn styles from Host.module.css, max-width 400px
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/HostSetup.jsx contains `export default function HostSetup`
    - src/pages/HostSetup.jsx contains `useParams` and extracts `sessionId`
    - src/pages/HostSetup.jsx contains "You're the host." or "You\u2019re the host."
    - src/pages/HostSetup.jsx contains `navigate(\`/host/${sessionId}\`)`
    - src/pages/HostSetup.jsx contains `supabase.from('sessions').update`
    - src/App.jsx contains `import HostSetup`
    - src/App.jsx contains `path="/host-setup/:sessionId"`
    - src/pages/Landing.jsx contains `navigate(\`/host-setup/${result.data.id}\`)`
    - src/pages/Landing.jsx does NOT contain `navigate(\`/host/${result.data.id}\`)`
    - vite build succeeds with exit code 0
  </acceptance_criteria>
  <done>HostSetup page renders room code + round selector + Open Lobby button. Landing redirects to /host-setup. App.jsx routes to HostSetup. Build passes.</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds with 0 errors
- All 4 new component files exist with correct exports
- HostSetup.jsx exists and is routed
- Landing.jsx redirects to /host-setup
- No existing functionality broken (Host.jsx, Play.jsx, PlayerRoster.jsx unchanged)
</verification>

<success_criteria>
- 4 leaf components (MeterBar, TimerDisplay, FrameworkLabel, CityPlaceholder) are implemented with CSS Modules and match UI-SPEC dimensions/colors/animations
- HostSetup page provides the host prep experience per D-08
- Routing updated: Landing -> /host-setup -> /host
- Vite build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-loop/03-01-SUMMARY.md`
</output>
