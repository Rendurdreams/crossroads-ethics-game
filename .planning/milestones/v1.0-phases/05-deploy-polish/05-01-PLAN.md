---
phase: 05-deploy-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/pages/HostSetup.jsx
  - src/pages/HostSetup.module.css
  - src/pages/Landing.jsx
  - index.html
autonomous: false
requirements: [INFRA-05]
user_setup:
  - service: netlify
    why: "Static hosting for the built app"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Netlify Dashboard -> Site settings -> Environment variables (documentation only — values baked in at build time from .env.local)"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Netlify Dashboard -> Site settings -> Environment variables (documentation only)"
    dashboard_config:
      - task: "Create free Netlify site and drag dist/ folder to deploy"
        location: "app.netlify.com -> Sites -> drag-and-drop"

must_haves:
  truths:
    - "vite build produces a dist/ folder with the app ready to deploy"
    - "Host setup page shows a QR code below the room code that encodes the join URL with ?code= prefilled"
    - "Scanning the QR code on a phone lands on the join page with the room code input already filled"
    - "index.html title reads 'The Crossroads' and has a meta description"
  artifacts:
    - path: "src/pages/HostSetup.jsx"
      provides: "QRCodeSVG component rendering join URL"
      contains: "QRCodeSVG"
    - path: "src/pages/Landing.jsx"
      provides: "URL query param pre-fill for room code"
      contains: "URLSearchParams"
    - path: "index.html"
      provides: "Correct page title and meta description"
      contains: "The Crossroads"
    - path: "dist/index.html"
      provides: "Built output"
  key_links:
    - from: "src/pages/HostSetup.jsx"
      to: "src/pages/Landing.jsx"
      via: "QR code URL with ?code= param"
      pattern: "window.location.origin.*code="
---

<objective>
Add QR code to host setup, wire Landing.jsx query param pre-fill, fix page metadata, install qrcode.react, and confirm vite build succeeds.

Purpose: Players can scan a QR code from the projected screen to join instantly. The app builds cleanly for Netlify deployment.
Output: Working QR code on HostSetup, pre-fill on Landing, clean dist/ build, checkpoint for manual Netlify deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-deploy-polish/05-CONTEXT.md
@.planning/phases/05-deploy-polish/05-RESEARCH.md
@.planning/phases/05-deploy-polish/05-UI-SPEC.md

@src/pages/HostSetup.jsx
@src/pages/HostSetup.module.css
@src/pages/Landing.jsx
@index.html
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install qrcode.react, add QR code to HostSetup, wire Landing query param, fix index.html</name>
  <files>package.json, src/pages/HostSetup.jsx, src/pages/HostSetup.module.css, src/pages/Landing.jsx, index.html</files>
  <read_first>
    - src/pages/HostSetup.jsx (current component structure — QR code goes after .roomCode div)
    - src/pages/HostSetup.module.css (current styles — add .qrCode and .qrInstruction classes)
    - src/pages/Landing.jsx (current useState for code — line 22: `const [code, setCode] = useState('')`)
    - index.html (current title is "crossroads-tmp" — needs update)
    - package.json (confirm qrcode.react not yet installed)
    - .planning/phases/05-deploy-polish/05-UI-SPEC.md (QR code visual contract)
    - .planning/phases/05-deploy-polish/05-CONTEXT.md (decisions D-05 through D-09, D-13)
  </read_first>
  <action>
    **Install qrcode.react:**
    Run `npm install qrcode.react@3` (per D-05, CLAUDE.md specifies 3.x).

    **HostSetup.jsx — add QR code (per D-05, D-06, D-08, D-09):**
    1. Add import at top: `import { QRCodeSVG } from 'qrcode.react'`
    2. After the `.roomCode` div (line 59) and before the `.instructions` paragraph, add:
       ```jsx
       <div className={styles.qrCode}>
         <QRCodeSVG
           value={`${window.location.origin}/?code=${session.room_code}`}
           size={180}
           bgColor="#12121e"
           fgColor="#f5f0e8"
         />
       </div>
       <p className={styles.qrInstruction}>Scan to join on your phone</p>
       ```

    **HostSetup.module.css — add QR styles (per UI-SPEC spacing):**
    Add after `.roomCode` block:
    ```css
    .qrCode {
      display: flex;
      justify-content: center;
      margin-top: 8px;
    }

    .qrInstruction {
      font-size: 14px;
      color: var(--text-muted);
      text-align: center;
      margin: 24px 0 0 0;
    }
    ```

    **Landing.jsx — query param pre-fill (per D-07):**
    Change line 22 from:
    `const [code, setCode] = useState('')`
    to:
    ```javascript
    const [code, setCode] = useState(() => {
      const params = new URLSearchParams(window.location.search)
      return params.get('code') ?? ''
    })
    ```

    **Landing.jsx — fix Join button copy (per UI-SPEC Copywriting Contract D-11):**
    Change the Join button text from `'Join'` to `'Join Game'` (line 149 — the text inside the ternary: `{joinLoading ? 'Joining...' : 'Join'}` becomes `{joinLoading ? 'Joining...' : 'Join Game'}`).

    **index.html — fix title and add meta (per D-13):**
    1. Change `<title>crossroads-tmp</title>` to `<title>The Crossroads</title>`
    2. Add after the viewport meta tag: `<meta name="description" content="A multiplayer ethics game for the classroom.">`
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "qrcode.react" in dependencies
    - src/pages/HostSetup.jsx contains `import { QRCodeSVG } from 'qrcode.react'`
    - src/pages/HostSetup.jsx contains `window.location.origin`
    - src/pages/HostSetup.jsx contains `bgColor="#12121e"`
    - src/pages/HostSetup.jsx contains `fgColor="#f5f0e8"`
    - src/pages/HostSetup.jsx contains `size={180}`
    - src/pages/HostSetup.module.css contains `.qrCode`
    - src/pages/HostSetup.module.css contains `.qrInstruction`
    - src/pages/Landing.jsx contains `new URLSearchParams(window.location.search)`
    - src/pages/Landing.jsx contains `params.get('code')`
    - src/pages/Landing.jsx contains `'Join Game'`
    - index.html contains `<title>The Crossroads</title>`
    - index.html contains `<meta name="description" content="A multiplayer ethics game for the classroom.">`
    - `npm run build` exits 0 and produces dist/index.html
  </acceptance_criteria>
  <done>QR code renders on HostSetup with correct colors/size, Landing pre-fills from ?code= param, Join button says "Join Game", index.html has correct title and meta, build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Netlify and verify public URL</name>
  <files>dist/</files>
  <action>
    Claude runs `npm run build` to produce the dist/ folder. Then the user deploys to Netlify:
    1. Go to app.netlify.com — create a new site by dragging the dist/ folder
    2. (Optional) In Netlify Site Settings -> Environment Variables, add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY for documentation (values already baked into bundle from .env.local at build time)
    3. Visit the deployed URL — confirm the landing page loads with title "The Crossroads"
    4. Navigate to host setup — confirm QR code appears below the room code
    5. Scan the QR code with a phone — confirm it opens the join page with the room code pre-filled
    6. Verify SPA routing works: refresh on /play/xxx — should not 404 (the _redirects file handles this)
  </action>
  <verify>User confirms deployed URL works with QR code, pre-fill, and SPA routing</verify>
  <done>App is live at a public Netlify URL. QR code works. SPA routing works. User types "approved" with URL.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- dist/index.html exists and contains "The Crossroads" in title
- HostSetup renders QRCodeSVG with session room code
- Landing reads ?code= from URL on mount
</verification>

<success_criteria>
- App builds and deploys to a public Netlify URL
- QR code on host setup encodes the join URL with room code
- Scanning QR code pre-fills room code on join page
- Page title is "The Crossroads" with meta description
</success_criteria>

<output>
After completion, create `.planning/phases/05-deploy-polish/05-01-SUMMARY.md`
</output>
