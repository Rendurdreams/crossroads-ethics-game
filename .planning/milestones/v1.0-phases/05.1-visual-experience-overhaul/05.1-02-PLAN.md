---
phase: 05.1-visual-experience-overhaul
plan: 02
type: execute
wave: 2
depends_on: [05.1-01]
files_modified:
  - src/components/ScenarioCard.module.css
  - src/components/ConsequenceReveal.module.css
  - src/components/ContentNote.module.css
  - src/components/MeterBar.module.css
  - src/components/FrameworkLabel.module.css
  - src/components/VoteTally.module.css
  - src/components/WorldStatePanel.module.css
  - src/components/PlayerRoster.module.css
  - src/components/CityPlaceholder.module.css
  - src/components/FrameworkProfile.module.css
  - src/pages/Landing.module.css
  - src/pages/HostSetup.module.css
  - src/pages/Host.module.css
  - src/pages/Play.module.css
autonomous: true
requirements: [D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "All card/panel containers use glass-morphism surface treatment instead of flat bg-surface"
    - "Choice buttons glow amber on hover and show bright glow ring when locked"
    - "Meter bar fills use warm amber gradient with glow shadow instead of flat color"
    - "Scenario text and consequence text display at 21px Playfair Display"
    - "FrameworkProfile .frameworkName uses Playfair Display 28px 700 instead of Inter 20px 600"
  artifacts:
    - path: "src/components/ScenarioCard.module.css"
      provides: "Glass card, upgraded choice buttons, 21px scenario text"
      contains: "backdrop-filter"
    - path: "src/components/MeterBar.module.css"
      provides: "Gradient fill with glow shadow"
      contains: "linear-gradient"
    - path: "src/components/FrameworkProfile.module.css"
      provides: "Glass sections, updated typography"
      contains: "var(--glass-bg)"
    - path: "src/pages/Host.module.css"
      provides: "Glass treatment on endPanel and reflectionCard"
      contains: "var(--glass-bg)"
  key_links:
    - from: "src/components/ScenarioCard.module.css"
      to: "src/index.css"
      via: "CSS custom properties"
      pattern: "var\\(--glass"
    - from: "src/components/MeterBar.module.css"
      to: "src/index.css"
      via: "gradient uses accent colors"
      pattern: "linear-gradient"
---

<objective>
Apply glass-morphism surface treatment, typography upgrades, meter gradient + glow, and choice button interaction spec across all component and page CSS module files.

Purpose: Transform the flat utilitarian dark theme into the cinematic glass-morphism aesthetic specified in the UI-SPEC. This is the bulk visual upgrade — every card, panel, meter, and button gets its premium treatment.
Output: All 14 CSS module files updated with glass surfaces, correct typography sizes/weights, gradient meter fills, and glow button interactions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-visual-experience-overhaul/05.1-UI-SPEC.md
@.planning/phases/05.1-visual-experience-overhaul/05.1-RESEARCH.md

<interfaces>
<!-- CSS tokens available from Plan 01 (in src/index.css :root) -->
--glow-amber: rgba(245, 158, 11, 0.15);
--glow-amber-strong: rgba(245, 158, 11, 0.35);
--glow-cool: rgba(59, 130, 246, 0.08);
--glass-bg: rgba(18, 18, 30, 0.65);
--glass-border: rgba(245, 158, 11, 0.18);
--glass-border-subtle: rgba(255, 255, 255, 0.06);
--blur-glass: 16px;
--serif: 'Playfair Display', Georgia, serif;  /* updated in Plan 01 */

<!-- Glass-morphism recipe (from UI-SPEC, apply to all card/panel containers) -->
background: var(--glass-bg);
backdrop-filter: blur(var(--blur-glass));
-webkit-backdrop-filter: blur(var(--blur-glass));
border: 1px solid var(--glass-border);
border-radius: 12px;
box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glass-morphism + typography on all 11 component CSS modules</name>
  <files>
    src/components/ScenarioCard.module.css,
    src/components/ConsequenceReveal.module.css,
    src/components/ContentNote.module.css,
    src/components/MeterBar.module.css,
    src/components/FrameworkLabel.module.css,
    src/components/VoteTally.module.css,
    src/components/WorldStatePanel.module.css,
    src/components/PlayerRoster.module.css,
    src/components/CityPlaceholder.module.css,
    src/components/FrameworkProfile.module.css,
    src/components/TimerDisplay.module.css
  </files>
  <read_first>
    - src/components/ScenarioCard.module.css
    - src/components/ConsequenceReveal.module.css
    - src/components/ContentNote.module.css
    - src/components/MeterBar.module.css
    - src/components/FrameworkLabel.module.css
    - src/components/VoteTally.module.css
    - src/components/WorldStatePanel.module.css
    - src/components/PlayerRoster.module.css
    - src/components/CityPlaceholder.module.css
    - src/components/FrameworkProfile.module.css
    - src/components/TimerDisplay.module.css
    - .planning/phases/05.1-visual-experience-overhaul/05.1-UI-SPEC.md (authoritative visual spec)
  </read_first>
  <action>
    Apply the following changes to each component CSS module. Every change below specifies the exact CSS property values. The glass recipe is:
    ```css
    background: var(--glass-bg);
    backdrop-filter: blur(var(--blur-glass));
    -webkit-backdrop-filter: blur(var(--blur-glass));
    border: 1px solid var(--glass-border);
    border-radius: 12px;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);
    ```

    **ScenarioCard.module.css:**
    - `.card`: Add glass recipe (currently has no background/border)
    - `.title`: Change `font-weight: 600` to `font-weight: 700` (two-weight system per UI-SPEC)
    - `.body`: Change `font-size: 18px` to `font-size: 21px`, change `line-height: 1.65` to `line-height: 1.75` (per D-06)
    - `.choiceBtn`: Replace `background: var(--bg-surface)` with `background: var(--glass-bg)`. Replace `border: 1px solid #2e303a` with `border: 1px solid var(--glass-border-subtle)`. Change `border-radius: 6px` to `border-radius: 8px`. Replace `transition` with `transition: border-color 200ms ease, box-shadow 200ms ease, transform 100ms ease` (per D-08)
    - `.choiceBtn:hover:not(:disabled)`: Replace `border-color: var(--text-muted)` with:
      ```css
      border-color: var(--glass-border);
      box-shadow: 0 0 12px rgba(245, 158, 11, 0.2);
      transform: translateY(-1px);
      ```
    - `.choiceLocked`: Replace entire rule with:
      ```css
      border-color: var(--accent) !important;
      box-shadow: 0 0 0 2px var(--accent), 0 0 20px var(--glow-amber-strong) !important;
      background: rgba(245, 158, 11, 0.12) !important;
      transform: none;
      cursor: default;
      ```
    - `.choiceDimmed`: Change `opacity: 0.4` to `opacity: 0.3`. Add `filter: grayscale(30%); transform: none;` (per D-08)

    **ConsequenceReveal.module.css:**
    - `.container`: Add a `.contentCard` sibling class (or update the container's children pattern). The `.container` itself stays as the full-page wrapper (keeps `background: var(--bg)`). The glass treatment goes on a new inner wrapper if needed, OR simply add glass recipe properties to `.consequence` parent context. Per UI-SPEC: "add glass recipe to the content wrapper (not the full-page background)". Add a new `.card` class with the glass recipe:
      ```css
      .card {
        background: var(--glass-bg);
        backdrop-filter: blur(var(--blur-glass));
        -webkit-backdrop-filter: blur(var(--blur-glass));
        border: 1px solid var(--glass-border);
        border-radius: 12px;
        padding: 24px;
        box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);
        max-width: 480px;
        width: 100%;
      }
      ```
      NOTE: The ConsequenceReveal.jsx will need a wrapper div with this class in Plan 03. For now, define the class. The existing CSS animations (fadeUp) remain — they will be preserved.
    - `.consequence`: Change `font-size: 18px` to `font-size: 21px`, change `line-height: 1.65` to `line-height: 1.75`

    **ContentNote.module.css:**
    - Add a new `.card` class with glass recipe (same as above) for the note content area. The `.overlay` stays as the full-page dark background.
    - No typography changes (body text stays Inter 16px 400 per UI-SPEC)

    **MeterBar.module.css:**
    - `.label`: Change `font-weight: 600` to `font-weight: 700`
    - `.value`: Change `font-weight: 600` to `font-weight: 700`
    - `.fill`: Replace `background: var(--accent)` with:
      ```css
      background: linear-gradient(90deg, #b45309 0%, #f59e0b 100%);
      box-shadow: 0 0 8px rgba(245, 158, 11, 0.4);
      ```
      Keep existing `transition: width 0.8s ease`. Add `, opacity 0.6s ease` to the transition. Per D-09.
    - `.fillDanger`: Replace `background: var(--danger)` with:
      ```css
      background: linear-gradient(90deg, #7f1d1d 0%, #ef4444 100%);
      box-shadow: 0 0 8px rgba(239, 68, 68, 0.4);
      ```

    **FrameworkLabel.module.css:**
    - `.badge`: Apply glass recipe. Replace `background: var(--accent-bg)` with `background: var(--glass-bg)`. Replace `border: 1px solid var(--accent)` with `border: 1px solid var(--glass-border)`. Add `backdrop-filter: blur(var(--blur-glass)); -webkit-backdrop-filter: blur(var(--blur-glass));`. Change `border-radius: 4px` to `border-radius: 8px`. Add `box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);`
    - `.name`: Change `font-weight: 600` to `font-weight: 700`

    **VoteTally.module.css:**
    - Add glass recipe to `.container`:
      ```css
      background: var(--glass-bg);
      backdrop-filter: blur(var(--blur-glass));
      -webkit-backdrop-filter: blur(var(--blur-glass));
      border: 1px solid var(--glass-border);
      border-radius: 12px;
      padding: 24px;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);
      ```
    - `.fill`: Replace `background: var(--accent)` with `background: linear-gradient(90deg, #b45309 0%, #f59e0b 100%);` and add `box-shadow: 0 0 8px rgba(245, 158, 11, 0.4);` (same gradient as MeterBar per UI-SPEC)
    - `.countPct`: Change `font-weight: 600` to `font-weight: 700`

    **WorldStatePanel.module.css:**
    - `.panel`: Replace `background: var(--bg-surface)` with glass recipe. Replace `border-left: 1px solid #2e303a` with `border: 1px solid var(--glass-border)`. Add `backdrop-filter: blur(var(--blur-glass)); -webkit-backdrop-filter: blur(var(--blur-glass));`. Add `box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);`
    - `.scenarioTitle`: Change `font-weight: 600` to `font-weight: 700`
    - `.actionBtn`: Change `font-weight: 600` to `font-weight: 700`

    **PlayerRoster.module.css:**
    - `.playerCard`: Replace `background: var(--bg-surface)` with `background: var(--glass-bg)`. Add `backdrop-filter: blur(var(--blur-glass)); -webkit-backdrop-filter: blur(var(--blur-glass));`. Replace `border-radius: 8px` with `border-radius: 8px`. Add `border: 1px solid var(--glass-border-subtle);`

    **CityPlaceholder.module.css:**
    - `.panel`: Apply glass recipe. Replace `background: var(--bg)` with `background: var(--glass-bg)`. Add `backdrop-filter: blur(var(--blur-glass)); -webkit-backdrop-filter: blur(var(--blur-glass)); border: 1px solid var(--glass-border); border-radius: 12px; box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);`

    **FrameworkProfile.module.css:**
    - `.sectionCard`: Replace `background: var(--bg-surface); border: 1px solid #2e303a; border-radius: 8px;` with glass recipe (background, backdrop-filter, -webkit-backdrop-filter, border with --glass-border, border-radius: 12px, box-shadow). Remove `opacity: 0; animation: fadeUp 400ms ease forwards; animation-delay: 0ms;` (Framer Motion will handle this in Plan 03)
    - `.frameworkName`: Change `font-family: var(--sans)` to `font-family: var(--serif)`. Change `font-size: 20px` to `font-size: 28px`. Change `font-weight: 600` to `font-weight: 700`. (Per UI-SPEC: "Inter 20px 600 -> Playfair Display 28px 700")
    - `.explanation`: Change `font-size: 18px` to `font-size: 21px`. Change `line-height: 1.65` to `line-height: 1.75`.
    - `.conflictSection`: Replace `background: var(--bg-surface); border: 1px solid #2e303a; border-radius: 8px;` with glass recipe. Remove `opacity: 0; animation: fadeUp 400ms ease forwards; animation-delay: 200ms;` (Framer Motion handles this in Plan 03)
    - `.leastUsedSection`: Replace `border-top: 1px solid #2e303a` with `border-top: 1px solid var(--glass-border-subtle)`. Remove `opacity: 0; animation: fadeUp 400ms ease forwards; animation-delay: 400ms;`
    - `.leastFrameworkName`: Change `font-weight: 600` to `font-weight: 700`
    - `.choiceLog`: Replace `border-top: 1px solid #2e303a` with `border-top: 1px solid var(--glass-border-subtle)`. Remove `opacity: 0; animation: fadeUp 400ms ease forwards; animation-delay: 600ms;`
    - `.choiceRow`: Replace `border-bottom: 1px solid #2e303a` with `border-bottom: 1px solid var(--glass-border-subtle)`
    - `.roundName`: Change `font-weight: 600` to `font-weight: 700`
    - Remove the `@media (prefers-reduced-motion: reduce)` block that set `animation: none; opacity: 1` on the four sections (no longer needed since CSS animations are removed)
    - Keep the `@keyframes fadeUp` definition (it may still be used elsewhere, remove in Plan 03 if confirmed unused)

    **TimerDisplay.module.css:**
    - No glass treatment per UI-SPEC ("No glass treatment - timer is typographic, not a card")
    - Check for any `font-weight: 600` and change to `700` if present
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && grep -c "var(--glass-bg)" src/components/ScenarioCard.module.css src/components/VoteTally.module.css src/components/WorldStatePanel.module.css src/components/FrameworkProfile.module.css src/components/PlayerRoster.module.css src/components/CityPlaceholder.module.css src/components/FrameworkLabel.module.css && grep -c "linear-gradient" src/components/MeterBar.module.css && grep "font-size: 21px" src/components/ScenarioCard.module.css src/components/ConsequenceReveal.module.css src/components/FrameworkProfile.module.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ScenarioCard.module.css .card contains `backdrop-filter: blur(var(--blur-glass))`
    - ScenarioCard.module.css .body contains `font-size: 21px` and `line-height: 1.75`
    - ScenarioCard.module.css .choiceBtn contains `background: var(--glass-bg)` and `border: 1px solid var(--glass-border-subtle)`
    - ScenarioCard.module.css .choiceLocked contains `box-shadow: 0 0 0 2px var(--accent), 0 0 20px var(--glow-amber-strong)`
    - ScenarioCard.module.css .choiceDimmed contains `opacity: 0.3` and `filter: grayscale(30%)`
    - ConsequenceReveal.module.css .consequence contains `font-size: 21px` and `line-height: 1.75`
    - MeterBar.module.css .fill contains `linear-gradient(90deg, #b45309 0%, #f59e0b 100%)` and `box-shadow: 0 0 8px rgba(245, 158, 11, 0.4)`
    - MeterBar.module.css .fillDanger contains `linear-gradient(90deg, #7f1d1d 0%, #ef4444 100%)`
    - FrameworkProfile.module.css .frameworkName contains `font-family: var(--serif)`, `font-size: 28px`, `font-weight: 700`
    - FrameworkProfile.module.css .sectionCard contains `var(--glass-bg)` and does NOT contain `animation: fadeUp`
    - FrameworkProfile.module.css .conflictSection does NOT contain `animation: fadeUp`
    - FrameworkProfile.module.css .leastUsedSection does NOT contain `animation: fadeUp`
    - FrameworkProfile.module.css .choiceLog does NOT contain `animation: fadeUp`
    - VoteTally.module.css .container contains `backdrop-filter`
    - WorldStatePanel.module.css .panel contains `var(--glass-bg)`
    - PlayerRoster.module.css .playerCard contains `var(--glass-bg)`
    - CityPlaceholder.module.css .panel contains `var(--glass-bg)`
    - No file contains `font-weight: 600` (all migrated to 700)
    - No file contains literal `border: 1px solid #2e303a` on card/panel elements (replaced with --glass-border or --glass-border-subtle)
  </acceptance_criteria>
  <done>All 11 component CSS modules updated with glass-morphism, gradient meters, glow buttons, and corrected typography</done>
</task>

<task type="auto">
  <name>Task 2: Glass-morphism + typography on all 4 page CSS modules</name>
  <files>
    src/pages/Landing.module.css,
    src/pages/HostSetup.module.css,
    src/pages/Host.module.css,
    src/pages/Play.module.css
  </files>
  <read_first>
    - src/pages/Landing.module.css
    - src/pages/HostSetup.module.css
    - src/pages/Host.module.css
    - src/pages/Play.module.css
    - .planning/phases/05.1-visual-experience-overhaul/05.1-UI-SPEC.md
  </read_first>
  <action>
    **Landing.module.css:**
    - `.page`: Add `position: relative; z-index: 1;` (ensures content sits above ambient layer)
    - `.section`: Apply glass recipe:
      ```css
      background: var(--glass-bg);
      backdrop-filter: blur(var(--blur-glass));
      -webkit-backdrop-filter: blur(var(--blur-glass));
      border: 1px solid var(--glass-border);
      border-radius: 12px;
      padding: 24px;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);
      ```
    - `.sectionHeading`: Change `font-weight: 600` to `font-weight: 700`
    - `.btn`: Change `font-weight: 600` to `font-weight: 700`
    - `.input`: Keep `background: var(--bg-surface)` (inputs do NOT get glass per UI-SPEC). Keep `border: 1px solid #2e303a` (inputs excluded from glass border migration).

    **HostSetup.module.css:**
    - `.page`: Add `position: relative; z-index: 1;`. Remove `background: var(--bg);` (let ambient layer show through, or keep it — the ambient layer is fixed/behind, but removing solid bg on `.page` helps glass children show the gradient bleed-through). Actually, per RESEARCH.md Pitfall 3: the ambientLayer is `position: fixed` so it underpaints everything regardless. The `.page` can keep its `background: var(--bg)` because glass containers within it will still blur the ambient layer through the stacking context. Keep `background: var(--bg)` for now.
    - `.heading`: Change `font-weight: 600` to `font-weight: 700`. Change `font-family: var(--sans)` to `font-family: var(--serif)` (page title per UI-SPEC heading tier: Playfair Display 28px 700).
    - `.roomCode`: Add `text-shadow: 0 0 20px rgba(245, 158, 11, 0.4);` for glow effect per UI-SPEC
    - `.roundBtnInactive`: Replace `border: 1px solid #2e303a` with `border: 1px solid var(--glass-border-subtle)`
    - `.roundBtnInactive:hover`: Replace `border-color: var(--accent)` — keep as is (accent border on hover is correct)
    - `.openBtn`: Change `font-weight: 600` to `font-weight: 700`

    **Host.module.css:**
    - `.page`: Add `position: relative; z-index: 1;` if not present
    - `.endPanel`: Replace `background: var(--bg-surface); border-left: 1px solid #2e303a;` with glass recipe:
      ```css
      background: var(--glass-bg);
      backdrop-filter: blur(var(--blur-glass));
      -webkit-backdrop-filter: blur(var(--blur-glass));
      border: 1px solid var(--glass-border);
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);
      ```
    - `.reflectionCard`: Replace `background: var(--bg); border: 1px solid #2e303a; border-radius: 6px;` with glass recipe (use border-radius: 12px)
    - `.endText`: Change `font-weight: 600` to `font-weight: 700`. Change to `font-family: var(--serif);` (heading tier)
    - `.frameworkNameLead`: Change `font-weight: 600` to `font-weight: 700`
    - `.frameworkNameNormal`: Change `font-weight: 600` to `font-weight: 700`
    - `.roundBtnInactive` (if present): Replace `border: 1px solid #2e303a` with `border: 1px solid var(--glass-border-subtle)`
    - `.startBtn`: Change `font-weight: 600` to `font-weight: 700`
    - Add `position: relative; z-index: 1;` to `.roundView` and `.endView` if they have `background: var(--bg)`

    **Play.module.css:**
    - `.page`: Add `position: relative; z-index: 1;`
    - `.name`: Change `font-weight: 600` to `font-weight: 700`
    - `.submittedNumber`: Change `font-weight: 600` to `font-weight: 700`
    - `.reflectionQuestion`: Change `font-size: 16px; line-height: 1.6` to `font-size: 21px; line-height: 1.75; font-family: var(--serif);` (prose text per UI-SPEC Scenario/Prose tier)
    - `.reflectionRoundQuestion`: Change `font-size: 18px; line-height: 1.65` to `font-size: 21px; line-height: 1.75` (prose tier)
    - `.reflectionSubmitBtn`: Change `font-weight: 600` to `font-weight: 700`
    - `.reflectionSection`: Remove `opacity: 0; animation: fadeUp 400ms ease forwards; animation-delay: 800ms;` (Framer Motion handles this in Plan 03). Set `opacity: 1;` explicitly.
    - Remove the `@media (prefers-reduced-motion: reduce)` block for `.reflectionSection` (no longer needed)
    - `.reflectionTextarea`: Keep `background: var(--bg-surface)` and `border: 1px solid #2e303a` (inputs NOT glass per UI-SPEC). Actually update border to `border: 1px solid var(--glass-border-subtle)` for visual consistency while keeping solid background.
    - `.gameContent`: Add `position: relative; z-index: 1;`
  </action>
  <verify>
    <automated>cd /Users/jay/MoralApp && grep -c "var(--glass-bg)" src/pages/Landing.module.css src/pages/Host.module.css && grep "font-size: 21px" src/pages/Play.module.css && grep "text-shadow" src/pages/HostSetup.module.css && ! grep "font-weight: 600" src/pages/Landing.module.css src/pages/HostSetup.module.css src/pages/Host.module.css src/pages/Play.module.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Landing.module.css .section contains `var(--glass-bg)` and `backdrop-filter`
    - Landing.module.css .input does NOT contain `var(--glass-bg)` (keeps solid bg)
    - HostSetup.module.css .roomCode contains `text-shadow: 0 0 20px rgba(245, 158, 11, 0.4)`
    - HostSetup.module.css .heading contains `font-family: var(--serif)` and `font-weight: 700`
    - Host.module.css .endPanel contains `var(--glass-bg)` and `backdrop-filter`
    - Host.module.css .reflectionCard contains `var(--glass-bg)` and `backdrop-filter`
    - Play.module.css .reflectionQuestion contains `font-size: 21px`
    - Play.module.css .reflectionSection does NOT contain `animation: fadeUp`
    - No page CSS file contains `font-weight: 600`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All 4 page CSS modules updated with glass surfaces, typography corrections, z-index layering for ambient layer, and font-weight migration to 700</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- No `.module.css` file in src/components/ or src/pages/ contains `font-weight: 600`
- All card/panel containers reference `var(--glass-bg)` and `backdrop-filter`
- Meter fills use `linear-gradient` instead of flat `var(--accent)`
- Scenario and consequence text at 21px
- FrameworkProfile sections have no CSS animation-delay rules
</verification>

<success_criteria>
- Glass-morphism surface treatment applied to all 12+ card/panel containers across components and pages
- Choice buttons have glass background, glow hover, bright locked glow ring, and dimmed state with grayscale
- Meter bar fills use amber gradient with glow shadow, danger state uses red gradient with glow
- All font-weight: 600 instances migrated to 700 (two-weight system)
- Scenario/consequence/explanation text upgraded to 21px Playfair Display
- FrameworkProfile.frameworkName at 28px Playfair Display 700
- CSS animation-delay stagger removed from FrameworkProfile (Framer Motion handles in Plan 03)
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-visual-experience-overhaul/05.1-02-SUMMARY.md`
</output>
